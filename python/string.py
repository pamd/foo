#!/usr/bin/python

s = "AbCDertRTGH"
s.lower() # Returns a string of all lowercase characters, but s IS NOT changed.

# s.format()
a = "Your name is {0} and your age is {age}"
a.format("Mike", age = 40)
 
